# AI Role Prep Hub

A privacy-neutral, gameful learning hub for people preparing for AI Solutions / Partner Engineering and AI GTM Engineering roles.

## Run locally

```bash
npm install
npm run dev
```

## Deploy

The included GitHub Actions workflow builds and deploys the site to GitHub Pages on pushes to `main`.

Progress and route selections remain in the browser's local storage. The app sends no data to a backend.
